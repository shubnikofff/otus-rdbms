version: "3.9"
services:
  master:
    build: ./master
    restart: always
    ports:
      - "5001:5432"
    environment:
      - POSTGRES_PASSWORD=otus
      - POSTGRES_HOST_AUTH_METHOD=md5

  physical_replica:
    build: ./physical_replica
    restart: always
    ports:
      - "5002:5432"
    depends_on:
      - master
    links:
      - master
    environment:
      - POSTGRES_PASSWORD=otus
      - POSTGRES_HOST_AUTH_METHOD=md5